political_actions = {

	join_eighth_coalition = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = SIB } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = republicanism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
			NOT = { ROOT = { has_country_flag = LEFT_EIGHTH_COALITION } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = SIB } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = republicanism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		NOT = { ROOT = { has_country_flag = LEFT_EIGHTH_COALITION } }
		}

		complete_effect = {
			SIB = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_zeitgeist_pact = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = POL } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = third_position }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
			NOT = { ROOT = { has_country_flag = LEFT_ZEITGEIST_PACT } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = POL } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = third_position }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		NOT = { ROOT = { has_country_flag = LEFT_ZEITGEIST_PACT } }
		}

		complete_effect = {
			POL = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_nouveau_regime = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = FRA } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = republicanism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
			NOT = { ROOT = { has_country_flag = LEFT_NOUVEAU_REGIME } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = FRA } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = republicanism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		NOT = { ROOT = { has_country_flag = LEFT_NOUVEAU_REGIME } }
		}

		complete_effect = {
			FRA = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 0
		}
	}
  join_socialist_international = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = BRI } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = socialism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
			NOT = { ROOT = { has_country_flag = LEFT_SOCIALIST_INTERNATIONAL } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = BRI } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = socialism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		NOT = { ROOT = { has_country_flag = LEFT_SOCIALIST_INTERNATIONAL } }
		}

		complete_effect = {
			BRI = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_african_union = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = ABY } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { has_country_flag = AFRICAN }
			NOT = { ROOT = { has_country_flag = LEFT_AFRICAN_UNION } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = ABY } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { has_country_flag = AFRICAN }
		NOT = { ROOT = { has_country_flag = LEFT_AFRICAN_UNION } }
		}

		complete_effect = {
			ABY = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_oldenburg_realms = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = DEN } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { has_country_flag = EUROPEAN }
			NOT = { ROOT = { has_country_flag = LEFT_OLDENBURG_REALMS } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = DEN } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { has_country_flag = EUROPEAN }
		NOT = { ROOT = { has_country_flag = LEFT_OLDENBURG_REALMS } }
		}

		complete_effect = {
			DEN = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_sacrum_imperium_romanum = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = HUN } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { has_country_flag = EUROPEAN }
			NOT = { ROOT = { has_country_flag = LEFT_SACRUM_IMPERIUM_ROMANUM } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = HUN } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { has_country_flag = EUROPEAN }
		NOT = { ROOT = { has_country_flag = LEFT_SACRUM_IMPERIUM_ROMANUM } }
		}

		complete_effect = {
			HUN = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_league_of_harmony_and_justice = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = PRC } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
			ROOT = { has_country_flag = ASIAN }
			NOT = { ROOT = { has_country_flag = LEFT_LEAGUE_OF_HARMONY_AND_JUSTICE } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = PRC } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { has_country_flag = ASIAN }
		NOT = { ROOT = { has_country_flag = LEFT_LEAGUE_OF_HARMONY_AND_JUSTICE } }
		}

		complete_effect = {
			PRC = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 80
		}
	}
  join_asian_muslim_league = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = BEN } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { has_country_flag = ASIAN }
      ROOT = { has_country_flag = MUSLIM }
			NOT = { ROOT = { has_country_flag = LEFT_ASIAN_MUSLIM_LEAGUE } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = BEN } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { has_country_flag = ASIAN }
		ROOT = { has_country_flag = MUSLIM }
		NOT = { ROOT = { has_country_flag = LEFT_ASIAN_MUSLIM_LEAGUE } }
		}

		complete_effect = {
			BEN = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_ottoman_empire = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = OTT } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { OR = {
      has_country_flag = AFRICAN
      has_country_flag = MIDDLE_EASTERN } }
      ROOT = { has_country_flag = MUSLIM }
			NOT = { ROOT = { has_country_flag = LEFT_OTTOMAN_EMPIRE } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = OTT } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { OR = {
		has_country_flag = AFRICAN
		has_country_flag = MIDDLE_EASTERN } }
		ROOT = { has_country_flag = MUSLIM }
		NOT = { ROOT = { has_country_flag = LEFT_OTTOMAN_EMPIRE } }
		}

		complete_effect = {
			OTT = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_dai_toa_kyoeiken = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = JAP } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = third_position }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { has_country_flag = ASIAN }
			NOT = { ROOT = { has_country_flag = LEFT_DAI_TOA_KYOEIKEN } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = JAP } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = third_position }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { has_country_flag = ASIAN }
		NOT = { ROOT = { has_country_flag = LEFT_DAI_TOA_KYOEIKEN } }
		}

		complete_effect = {
			JAP = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_imperio_portugues = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = SSA } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { NOT = { has_country_flag = EUROPEAN } }
			NOT = { ROOT = { has_country_flag = LEFT_IMPERIO_PORTUGUES } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = SSA } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { NOT = { has_country_flag = EUROPEAN } }
		NOT = { ROOT = { has_country_flag = LEFT_IMPERIO_PORTUGUES } }
		}

		complete_effect = {
			SSA = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_imperio_espanol = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = SPA } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN }
      ROOT = { NOT = { has_country_flag = EUROPEAN } }
			NOT = { ROOT = { has_country_flag = LEFT_IMPERIO_ESPANOL } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
		NOT = { ROOT = { has_war_with = SPA } }
		ROOT = { is_in_faction = no }
		ROOT = { has_government = monarchism }
		ROOT = { is_subject = no }
		ROOT = { has_country_flag = PATH_CHOSEN }
		ROOT = { NOT = { has_country_flag = EUROPEAN } }
		NOT = { ROOT = { has_country_flag = LEFT_IMPERIO_ESPANOL } }
		}

		complete_effect = {
			SPA = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 20
		}
	}
  boost_stability = {

		icon = generic_political_discourse

		available = {

		}

		visible = {
			always = yes
		}

		fire_only_once = no

		modifier = {
			stability_weekly = 0.01
		}

		days_remove = 180
		days_re_enable = 180

		cost = 90

		complete_effect = {

		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_stability < 0.6
			}
		}
	}
  boost_war_support = {

		icon = generic_political_discourse

		available = {
			has_war_support < 0.9
		}

		visible = {
			always = yes
		}

		remove_trigger = {
		}

		fire_only_once = no

		modifier = {
			war_support_weekly = 0.01
		}

		days_remove = 180
		days_re_enable = 180

		cost = 90

		complete_effect = {

		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_war_support < 0.5
			}
		}
	}
}
