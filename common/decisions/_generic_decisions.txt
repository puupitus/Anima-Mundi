political_actions = {

	join_eighth_coalition = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = SIB } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = republicanism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			SIB = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_zeitgeist_pact = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = POL } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = third_position }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			POL = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_nouveau_regime = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = FRA } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = republicanism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			FRA = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 0
		}
	}
  join_socialist_international = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = BRI } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = socialism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			BRI = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_african_union = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = ABY } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { has_country_flag = AFRICAN }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			ABY = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_oldenburg_realms = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = DEN } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { has_country_flag = EUROPEAN }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			DEN = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_sacrum_imperium_romanum = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = HUN } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { has_country_flag = EUROPEAN }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			HUN = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_league_of_harmony_and_justice = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = PRC } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { has_country_flag = ASIAN }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			PRC = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 80
		}
	}
  join_asian_muslim_league = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = BEN } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { has_country_flag = ASIAN }
      ROOT = { has_country_flag = MUSLIM }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			BEN = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_ottoman_empire = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = OTT } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { OR = {
      has_country_flag = AFRICAN
      has_country_flag = MIDDLE_EASTERN } }
      ROOT = { has_country_flag = MUSLIM }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			OTT = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_dai_toa_kyoeiken = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = JAP } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = third_position }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { has_country_flag = ASIAN }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			JAP = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_imperio_portugues = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = SSA } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { NOT = { has_country_flag = EUROPEAN } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			SSA = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 50
		}
	}
  join_imperio_espanol = {

		icon = generic_decision

		allowed = {
		}

		available = {
      NOT = { ROOT = { has_war_with = SPA } }
			ROOT = { is_in_faction = no }
      ROOT = { has_government = monarchism }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
      ROOT = { NOT = { has_country_flag = EUROPEAN } }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction = no }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			SPA = { add_to_faction = ROOT }
		}

		ai_will_do = {
			factor = 20
		}
	}
  boost_stability = {

		icon = generic_political_discourse

		available = {

		}

		visible = {
			always = yes
		}

		fire_only_once = no

		modifier = {
			stability_weekly = 0.01
		}

		days_remove = 180
		days_re_enable = 180

		cost = 90

		complete_effect = {

		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_stability < 0.6
			}
		}
	}
  boost_war_support = {

		icon = generic_political_discourse

		available = {
			has_war_support < 0.9
		}

		visible = {
			always = yes
		}

		remove_trigger = {
		}

		fire_only_once = no

		modifier = {
			war_support_weekly = 0.01
		}

		days_remove = 180
		days_re_enable = 180

		cost = 90

		complete_effect = {

		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_war_support < 0.5
			}
		}
	}
  leave_eighth_coalition = {

		icon = generic_decision

		allowed = {
		}

		available = {
			ROOT = { is_in_faction_with = SIB }
      ROOT = { NOT = { has_government = republicanism } }
      ROOT = { is_subject = no }
      ROOT = { has_country_flag = PATH_CHOSEN_2 }
		}

		#If this trigger matches a country.
		target_trigger = {
				}


		visible = {
    ROOT = { is_in_faction_with = SIB }
    ROOT = { is_subject = no }
		}

		complete_effect = {
			SIB = { remove_from_faction = ROOT }
		}

		ai_will_do = {
			factor = 20
		}
	}
}
